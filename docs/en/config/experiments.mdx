# Experiments

Enable and try out some experimental features.

- **Type：** `Object`

## experiments.incrementalRebuild

Enable incremental rebuild. `true` will try to reuse the results of the last build when rebuild to improve build speed.

- **Type：** `boolean`
- **Default：** `true`
